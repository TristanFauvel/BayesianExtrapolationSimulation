% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_conjugate_inference.R
\name{normal_mixture_posterior}
\alias{normal_mixture_posterior}
\title{Conjugate update of a normal mixture prior across replicates}
\usage{
normal_mixture_posterior(weights, means, sds, estimate, standard_error)
}
\arguments{
\item{weights}{Prior component weights. Either a vector of length
\code{n_components} (a prior shared by every replicate) or a
\verb{n_replicates x n_components} matrix (one prior per replicate, as needed by
empirical Bayes methods).}

\item{means}{Prior component means, shaped like \code{weights}.}

\item{sds}{Prior component standard deviations, shaped like \code{weights}.}

\item{estimate}{Vector of per-replicate treatment effect estimates.}

\item{standard_error}{Vector of per-replicate standard errors.}
}
\value{
A list of three \verb{n_replicates x n_components} matrices: \code{weights},
\code{means} and \code{sds} of the posterior mixture.
}
\description{
Applies the normal-normal conjugate update to every replicate at
once. This is the vectorised equivalent of calling \code{\link[RBesT:postmix]{RBesT::postmix()}} once per
replicate: for a prior component with weight \eqn{w_k}, mean \eqn{\mu_k} and
standard deviation \eqn{s_k}, and an observation \eqn{(m_i, se_i)}, the
posterior component has variance \eqn{1/(1/s_k^2 + 1/se_i^2)}, mean
\eqn{v_{ik}(\mu_k/s_k^2 + m_i/se_i^2)} and weight proportional to
\eqn{w_k \, N(m_i; \mu_k, s_k^2 + se_i^2)}.
}
