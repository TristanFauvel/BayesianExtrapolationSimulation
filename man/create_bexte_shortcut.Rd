% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_app.R
\name{create_bexte_shortcut}
\alias{create_bexte_shortcut}
\title{Add a desktop shortcut that launches the app}
\usage{
create_bexte_shortcut(
  workspace = NULL,
  applications_dir = path.expand("~/.local/share/applications"),
  script_dir = tools::R_user_dir("BExTE", "data")
)
}
\arguments{
\item{workspace}{Workspace to launch with, or \code{NULL} to let
\code{\link[=run_bexte_app]{run_bexte_app()}} choose one.}

\item{applications_dir}{Directory holding desktop entries. The default puts
the entry in the application menu, which, unlike one placed on the
desktop, needs no separate step to mark it trusted.}

\item{script_dir}{Directory to write the launcher script into.}
}
\value{
Paths of the launcher and the desktop entry, invisibly.
}
\description{
Writes an application-menu entry that starts the Shiny app, so
the app can be launched without an R session. It shortens \emph{launching}, not
\emph{installing}: R, BExTE and CmdStan all still have to be present, which is
what \code{inst/scripts/install.R} is for.

Two files are written. A shell script holds the R call, because the
\code{Exec} key of a desktop entry is split on whitespace rather than parsed by
a shell, so quotes around an \code{-e} argument would reach R intact. The entry
then points at that script.

The entry runs in a terminal on purpose. The app is a server that runs
until it is stopped, and the terminal is what carries the workspace path,
the progress of a run, and Ctrl+C as the way to stop it.
}
