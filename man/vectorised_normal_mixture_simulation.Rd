% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_conjugate_inference.R
\name{vectorised_normal_mixture_simulation}
\alias{vectorised_normal_mixture_simulation}
\title{Run a whole normal-mixture simulation without looping over replicates}
\usage{
vectorised_normal_mixture_simulation(
  weights,
  means,
  sds,
  samples,
  target_data,
  to_return,
  critical_value,
  theta_0,
  confidence_level,
  null_space,
  posterior_parameters = NULL,
  posterior = NULL,
  mcmc = FALSE
)
}
\arguments{
\item{weights}{Prior component weights, shared across replicates (a vector)
or one row per replicate (a matrix).}

\item{means}{Prior component means, shaped like \code{weights}.}

\item{sds}{Prior component standard deviations, shaped like \code{weights}.}

\item{samples}{Data frame of generated replicates, with columns
\code{treatment_effect_estimate}, \code{treatment_effect_standard_error} and
\code{standard_deviation}.}

\item{target_data}{Target data object, used for its sample size per arm.}

\item{to_return}{Character vector of requested outputs.}

\item{critical_value}{Critical value for the test decision.}

\item{theta_0}{Null hypothesis value.}

\item{confidence_level}{Credible interval level.}

\item{null_space}{Either \code{"left"} or \code{"right"}.}

\item{posterior_parameters}{Optional data frame of per-replicate posterior
parameters to report.}

\item{posterior}{Optional output from \code{\link[=normal_mixture_posterior]{normal_mixture_posterior()}} when the
caller already needed it for method-specific parameter summaries.}

\item{mcmc}{Whether the calling model samples when it is not on this fast
path, which decides what the MCMC diagnostics report.}
}
\value{
A list shaped like the return value of
\code{\link[=Model$simulation_for_given_treatment_effect]{Model$simulation_for_given_treatment_effect()}}.
}
\description{
Shared implementation behind the vectorised fast path. Given the
prior mixture for every replicate, this reproduces exactly what the replicate
loop would have produced: posterior moments, medians, credible intervals,
test decisions and effective sample sizes.
}
\keyword{internal}
