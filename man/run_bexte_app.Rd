% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_app.R
\name{run_bexte_app}
\alias{run_bexte_app}
\title{Launch the BExTE Shiny app}
\usage{
run_bexte_app(workspace = NULL, ...)
}
\arguments{
\item{workspace}{Directory holding the simulation output. Defaults to the
repository root in a source checkout and to
\code{tools::R_user_dir("BExTE", "data")} otherwise.}

\item{...}{Passed on to \code{shiny::runApp()} (e.g. \code{port}, \code{launch.browser}).}
}
\value{
Does not return; runs the app until interrupted.
}
\description{
Opens a browser-based app to configure a simulation study
(case studies, methods, parameter grids), run it locally, and then
analyze and visualize the results.

The app reads and writes \verb{results/<env>/}, \verb{logs/<env>/} and
\verb{user_configs/<env>/} inside \code{workspace}. Launched from a source checkout
it uses the checkout, so results land next to the ones
\verb{Rscript inst/scripts/main.R} produces; installed from a release tarball
it uses a per-user directory under \code{\link[tools:R_user_dir]{tools::R_user_dir()}}. Either way the
directory it settled on is reported when the app starts.
}
