% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_prior_ess_estimation.R
\name{truncated_normal_mixture_elir}
\alias{truncated_normal_mixture_elir}
\title{Expected local information ratio ESS of a truncated normal mixture}
\usage{
truncated_normal_mixture_elir(weights, means, sds, lower, upper, sigma)
}
\arguments{
\item{weights}{Component weights of the mixture, summing to one.}

\item{means}{Component means.}

\item{sds}{Component standard deviations.}

\item{lower}{Lower truncation point.}

\item{upper}{Upper truncation point.}

\item{sigma}{Reference scale of the normal likelihood.}
}
\value{
The ELIR effective sample size.
}
\description{
The ELIR effective sample size of a prior \eqn{\pi} against a
normal likelihood of known scale \eqn{\sigma} is
\deqn{\sigma^2 \, E_\pi[-(\log \pi)''(\theta)],}
which is what \code{\link[RBesT:ess]{RBesT::ess()}} evaluates for an untruncated normal mixture.

A normal mixture truncated to an interval has no representation as an
untruncated mixture, so the alternative is to fit one to a sample drawn from
it. That costs a mixture fit per replicate and leaves both Monte Carlo noise
and an upward bias in the result. This function integrates the definition
directly instead.

Each component is truncated and renormalised on its own, matching how the
robust mixture prior is drawn from. Inside the interval the truncation is a
constant factor on the density, so it does not contribute to the second
derivative of the log density; it enters only through the normalisers and the
domain of integration.
}
