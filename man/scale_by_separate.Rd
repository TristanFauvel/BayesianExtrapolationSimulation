% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_methods_comparison_forestplot.R
\name{scale_by_separate}
\alias{scale_by_separate}
\title{Express a metric as a ratio to the separate analysis}
\usage{
scale_by_separate(df, metric_columns)
}
\arguments{
\item{df}{A frequentist results frame containing \code{separate} method rows.}

\item{metric_columns}{Character vector of columns to divide - typically the
metric and its two confidence bounds.}
}
\value{
\code{df} with \code{metric_columns} divided by the separate analysis's value,
in the original column order.
}
\description{
Divides each row's metric by the value the \code{separate} (no
borrowing) method takes in the same scenario. The scenario key is the full
set of coordinates that identify a simulated configuration, so the ratio
never collapses across drift or sample size.

A zero or missing denominator makes the ratio undefined. Those rows are
dropped with a warning rather than reported as \code{Inf} or as some finite
stand-in.
}
