% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_simulation_env.R
\name{run_simulation_env}
\alias{run_simulation_env}
\title{Run a full simulation for one environment}
\usage{
run_simulation_env(
  env,
  config_dir,
  case_studies_config_dir,
  simulation_config,
  analysis_config,
  frequentist_metrics,
  inference_metrics,
  results_dir = paste0("./results/", env, "/"),
  check_results_completeness = TRUE
)
}
\arguments{
\item{env}{The environment name (used to name the \verb{./logs/<env>/} and
\verb{./results/<env>/} directories).}

\item{config_dir}{Directory containing \code{scenarios_config.yml},
\code{mcmc_config.yml} and \code{methods_config.R} for this environment (must end
with a trailing slash).}

\item{case_studies_config_dir}{Directory containing the case study YAML
files referenced by this environment (must end with a trailing slash).}

\item{simulation_config}{Simulation configuration list (as read from
\code{simulation_config.yml}).}

\item{analysis_config}{Analysis configuration list (as read from
\code{analysis_config.yml}).}

\item{frequentist_metrics}{List of frequentist metrics, as defined by
sourcing \code{metrics_config.R}.}

\item{inference_metrics}{List of inference metrics, as defined by sourcing
\code{metrics_config.R}.}

\item{results_dir}{Directory the results are written to. Defaults to
\verb{./results/<env>/}.}

\item{check_results_completeness}{Reserved for a future completeness
check; currently unused, mirroring \code{inst/scripts/main.R} where the
corresponding call is commented out.}
}
\value{
\code{TRUE}, invisibly, once frequentist and/or Bayesian OCs have been
computed and concatenated according to \code{simulation_config}.
}
\description{
Runs the frequentist and/or Bayesian Monte Carlo operating
characteristics simulation for a single environment. This mirrors the
per-environment body of the driver loop in \code{inst/scripts/main.R}, extracted
into a callable function so it can be invoked directly (e.g. from a
background process launched by the Shiny app).
}
