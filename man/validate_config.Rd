% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_validation.R
\name{validate_config}
\alias{validate_config}
\title{Validate a configuration read from YAML}
\usage{
validate_config(config, schema, context)
}
\arguments{
\item{config}{The list returned by \code{\link[yaml:read_yaml]{yaml::read_yaml()}}.}

\item{schema}{A named list mapping keys to a type in
\link{config_type_predicates}. Keys absent from the schema are not checked.}

\item{context}{The name of the configuration file, used in the error.}
}
\value{
No return value, called for side effects.
}
\description{
YAML enters the package as an untyped nested list, so a typo in
a key or a value of the wrong shape otherwise surfaces as a \code{NULL}
propagating into arithmetic far from the file that caused it. This checks a
config against a schema at the point it is read, so the error names the
file, the key and what was expected.
}
\keyword{internal}
