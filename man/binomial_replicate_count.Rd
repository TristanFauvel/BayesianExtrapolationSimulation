% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty_propagation.R
\name{binomial_replicate_count}
\alias{binomial_replicate_count}
\title{Recover the number of Monte Carlo replicates behind a binomial estimate}
\usage{
binomial_replicate_count(
  estimate,
  mcse,
  conf_int_lower,
  conf_int_upper,
  conf_level = 0.95
)
}
\arguments{
\item{estimate}{The estimated proportion.}

\item{mcse}{The Monte Carlo standard error of the estimate, or \code{NA}.}

\item{conf_int_lower}{Lower bound of the exact (Clopper-Pearson) interval.}

\item{conf_int_upper}{Upper bound of the exact (Clopper-Pearson) interval.}

\item{conf_level}{The confidence level the interval was computed at.}
}
\value{
The number of replicates, or \code{NA_real_} when no summary identifies it.
}
\description{
Operating characteristics are stored as summaries (estimate,
Monte Carlo standard error, exact confidence interval) rather than as the
underlying counts. Propagating their uncertainty correctly requires the
replicate count, which this function recovers from whichever summary is
informative.

The Monte Carlo standard error identifies the replicate count exactly
whenever it is available and non-zero. It is zero precisely when no
replicate succeeded or all of them did, and in that case one bound of the
exact interval is a closed-form function of the replicate count alone. As a
last resort, when no standard error was stored for an interior estimate,
the count is approximated from the width of the interval.
}
