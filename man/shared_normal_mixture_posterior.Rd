% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vectorised_conjugate_inference.R
\name{shared_normal_mixture_posterior}
\alias{shared_normal_mixture_posterior}
\title{Conjugate update of a normal mixture prior shared by every replicate}
\usage{
shared_normal_mixture_posterior(weights, means, sds, estimate, standard_error)
}
\arguments{
\item{weights}{Prior component weights, a vector of length \code{n_components}.}

\item{means}{Prior component means, shaped like \code{weights}.}

\item{sds}{Prior component standard deviations, shaped like \code{weights}.}

\item{estimate}{Vector of per-replicate treatment effect estimates.}

\item{standard_error}{Per-replicate standard errors, already recycled to the
same length as \code{estimate}.}
}
\value{
A list of three \verb{n_replicates x n_components} matrices: \code{weights},
\code{means} and \code{sds} of the posterior mixture.
}
\description{
The same update as \code{\link[=normal_mixture_posterior]{normal_mixture_posterior()}}, for the common
case of one prior serving every replicate. Broadcasting the component values
against the observations with \code{\link[=outer]{outer()}} keeps three constant
\verb{n_replicates x n_components} copies of the prior out of memory. For the
commensurate quadrature mixture those copies alone run to hundreds of
megabytes; the posterior matrices are irreducible and dominate what is left.
}
\keyword{internal}
