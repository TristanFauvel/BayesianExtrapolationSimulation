# Function to return a dataframe of parameters from json strings

This function returns, for a dataframe containing parameters (in json
strings) for a given method, an R dataframe.

## Usage

``` r
get_parameters(parameters_df)
```

## Arguments

- parameters_df:

  The dataframe containing parameters in JSON strings.

## Value

An R dataframe with parsed parameters.
